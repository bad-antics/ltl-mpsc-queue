// examples/demo.ltl — runnable demo for ltl_mpsc_queue

import std::io
use ltl_mpsc_queue as lib

fn main() {
    io::println("ltl_mpsc_queue demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}